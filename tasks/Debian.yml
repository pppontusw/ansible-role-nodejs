- apt_key:
    url: https://deb.nodesource.com/gpgkey/nodesource.gpg.key
- apt_repository:
    repo: 'deb https://deb.nodesource.com/node_7.x xenial main'
    state: present
- shell: apt-get update && apt-get install nodejs git -y
  become: yes